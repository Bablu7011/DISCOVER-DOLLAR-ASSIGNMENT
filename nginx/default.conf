server {
    listen 80;

    # Serve Angular frontend
    location / {
        proxy_pass http://frontend:80;
    }

    # Backend API
    location /api/ {
        proxy_pass http://backend:8080/api/;
    }
}
